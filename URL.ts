type urlsType = { [key: string]: string };

const urls = {
  HOME: "/",
  DISCOVER: "/discover",
  COLLECTIONS: "/collections",
  DOWNLOADS: "/downloads",
  FAVOURITES: "/favourites",
  LOCAL_FILES: "/local-files",
  SETTINGS: "/settings",
  PROFILE: "/profile",
  LOGOUT: "/logout",
} as urlsType;

export default urls;
